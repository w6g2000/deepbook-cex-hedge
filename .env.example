# Optional path to override the default runtime config (config/app.toml).
#APP_CONFIG_PATH=/absolute/path/to/config/app.toml

# Required: Binance API credentials for spot trading.
BINANCE_API_KEY=replace-with-api-key
BINANCE_API_SECRET=replace-with-api-secret
# Optional: Binance 现货充值地址（备查/人工校验用，程序不会主动发起充值）。
BINANCE_SPOT_DEPOSIT_ADDRESS=0x...
# Optional: 如充值网络需要 Memo/Tag，请在此填充提示。
#BINANCE_SPOT_DEPOSIT_MEMO=123456
# Optional: Binance 现货提现触发阈值（基点），留空则禁用自动提现逻辑。
#SPOT_WITHDRAW_THRESHOLD_BPS=150

# Required: Sui secret key used for DeepBook on-chain orders.
DEEPBOOK_PRIVATE_KEY=replace-with-hex-or-sui-keypair
# Required: DeepBook balance manager object ID (used by SDK backend).
DEEPBOOK_BALANCE_MANAGER_ID=0x...

# Required: Sui secret key for Navi lending operations (bech32 or base64).
NAVI_SIGNER_KEY=replace-with-sui-private-key
